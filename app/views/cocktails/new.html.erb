<%= simple_form_for(@cocktail) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>

  <%= f.submit %>
<% end %>
<%= link_to 'Back to cocktail', root_path, class: "btn btn-primary" %>
